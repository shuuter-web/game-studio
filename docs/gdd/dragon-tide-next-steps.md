# Dragon Tide — 検討中の方針（次スレッド引き継ぎ）

- 作成: 2026-07-30 ／ 全面更新: 2026-08-12（v0.36.2）／ **更新: 2026-08-25（v0.48.0 時点）**
- 対象コード: `prototypes/dragon-tide/index.html`
- この文書は**残作業と検討中アイディアの保管**。実装済みの living doc は下記を参照。
- 行番号は変動するため**識別子（関数名・定数名）で参照**する。最新の実像はコードを正とする。

## 関連ドキュメント（役割分担）

| 文書 | 役割 |
|---|---|
| [dragon-tide-species.md](dragon-tide-species.md) | **竜種15種の案**（攻撃×動き×見た目）。攻撃カタログ24種・決定事項・進め方 |
| [docs/ui/dragon-tide-controls.md](../ui/dragon-tide-controls.md) | **操作の仕様と実装**。動きパラメータ（§12）・外力チャネル（§12-2c）・検証の罠 |
| [docs/tdd/dragon-tide-sprites.md](../tdd/dragon-tide-sprites.md) | **スプライト生成方針**。パーツ分割カテゴリA〜G・アンカーの決め方・プロンプト |
| [dragon-tide-meta.md](dragon-tide-meta.md) | メタ進行（ツリー・チャレンジ・カード）の living doc |
| [dragon-tide-era-run.md](dragon-tide-era-run.md) | **ラン構成**（1ラン＝1文明／時代はフレーバー・強化は動員段階）。v0.56.0 で確定 |
| 本書 | 残作業の一覧と、まだ着手していないアイディア |

---

## 1. 現在地（v0.48.0）

| 領域 | 状態 |
|---|---|
| **竜種** | **13種**が選択可（v0.45で☄️彗星竜＝UFOと動きが完全同一・尻尾ありの別種を追加）。火竜/氷竜/影竜/緋竜（動きは共通のスタンダード）＋隼竜/燕竜/蜂竜/ガーゴイル（v0.35・動きが異なる）＋**UFO/爆竜/蛙竜（v0.37・極端枠）**＋**惑竜（v0.40・爆竜と同一の体で操作だけ旧方式）** |
| **攻撃** | 10種（炎息・氷槍・影弾・爪撃・刃翼・羽根散弾・衛星蜂・落下叩きつけ・**絨毯爆撃**・**舌撃**）。`ATTACK_DEFS` で竜種から独立 |
| **構造** | `SPECIES_DEFS`（攻撃×動き×見た目）／`ATTACK_DEFS`（kind・反動・硬直）／`DRAGON_PART_SPRITES`（パーツ座標） |
| **動き** | 極座標積分（向きと速さを別々に制限）。`turnRate`/`accel`/`minSpeed`/`leaderSpeed`/`leaderPull`/`pathEndMode`＋**`orbitRadius`/`hop*`/`pathTrace`（v0.37）** |
| **操作** | 「指が常に操縦点」。指を止めれば止まる。パス末端の挙動は竜種別（loop/wander/straight/stop/**orbit**）。**UFO だけ操舵をやめて線を弧長でなぞる**（`pathTrace`）。**爆竜だけ操作方式そのものが違う**＝なぞって離すと矢印の向きに進路が確定する（`controlScheme:"vector"`） |
| **反動/ノックバック** | 減衰する外力チャネル（`boidKickX/Y`）に統一。位置ワープなし。氷槍の反動は28（実機OK） |
| **アセット** | 竜4種が専用のパーツ分離アセット（実機OK）。新7種はベクター描画＋専用パレット（暫定） |
| **スプライト管理** | 種IDキーの簡易LRU（`getSpeciesSprites`・上限3種） |
| **UI** | 竜種選択は11種になったので**2列グリッド**（`#card-list.species-grid`）。375×812で全カードが画面内＋タップ到達を実DOMで確認済み |
| メタ進行 | 永続プロファイル／恒常ツリー5ライン／チャレンジ6種／母竜ブレス（ラン中★カードで取得） |
| 攻略対象 | 花火要塞／エリート／財宝馬車／巨獣／衝撃兵・治癒塔・召喚門／2分間隔のラッシュ<br>**v0.42: 鏡の盾兵／分裂体／潜行者／火吹き車／瘴気塔／焼夷塔（建物）**<br>**v0.43: ダム／堰（決壊で下流が水没）／雷撃塔車（密集すると痛い連鎖電撃）** |
| **持続範囲攻撃（v0.42）** | 危険地帯システム（`hazards`）。敵も建物も同じ仕組みで「地面に居座る帯」を置ける。0.7秒の予告 → 0.25秒刻みでダメージ |
| **ボス周辺（v0.41）** | 城下町を**マップで最も濃い市街**にした（半径260px内 157〜173棟。最も濃い街は120〜177棟＝ほぼ同等、半径400pxでは1.4〜3.0倍）。**城壁の内側を巡回する守備隊**を新設（8〜21体・HP192〜2180） |

### v0.36.2 の実機判定（2026-08-14・完了）

| 確認項目 | 結果 |
|---|---|
| 竜4種の見た目 | **問題なし**（v0.36.0〜v0.36.2 の作り直しは通った） |
| 動きの違いが手触りを変えるか | **どれもうまく行っている。ただしもっと極端なものも試したい** → v0.37 の3種へ |
| 氷槍の反動28 | **問題なし**（この値で確定） |

---

## 2. 次にやること（優先順）

### 2-0. ★★ ラン構成の再設計（最優先・2026-08-29 方針確定）

**1ラン＝1大陸。破壊率で敵文明が時代を進め、目の前で町が建て替わる。**
設計は [dragon-tide-era-run.md](dragon-tide-era-run.md) に確定済み。以下の作業順で進める。

1. レイヤーのタイル化（見た目は変わらないのが正解。ワールド拡大の前提）
2. ワールド拡大 9600×7200 と大陸生成
3. 時代進行システム（破壊率60%トリガー・建て替え・復興・難度付け替え・群れ上限解放）
4. ラン構成の入れ替え（開始時代の選択と解放・セーブ・リザルト）
5. 調整

確定した論点: 遷移の引き金＝**破壊率30%** / 群れは**削らない**（代わりに上限解放） /
攻め込む文明は**クリアで解放** / **時代はラン中固定のフレーバー、強化は動員段階**（v0.56）。
フェーズ1〜5は完了済み。残るのは実機の体感による調整。


### 2-1. ★実機での確認待ち（最優先・これが通らないと先に進めない）

**v0.37.1 の極端枠3種**。動きの判定は1周した（下表）ので、**次は攻撃と組み合わせた手触り**を見る。

| 竜種 | 直近の判定 | v0.39 で変えたこと | 見るポイント |
|---|---|---|---|
| 💣 **爆竜** | 「いい感じ。**まっすぐ進み続けて爆撃し続けるので、むしろこいつが一番アクアリウムを実現している**」 | 確定後の矢印を**2.2秒で消える**ようにした（最後の0.7秒でフェード。縁で進路が変わったときは出し直す） | 矢印が消えるタイミングが早すぎ／遅すぎないか。放置して眺めたときの気持ちよさ |
| 🌀 **惑竜**（新） | 「**前のバージョンの操作がまごつく感じも良かった**」 | 旧操作の爆竜を**別種として復活**。動き・攻撃は爆竜と完全に同一で、`controlScheme` だけが "path"（線をなぞる） | 爆竜と続けて遊んで、**操作方式の違いだけでどれだけ別のゲームになるか**。まごつきが持ち味として成立するか |
| 🛸 **UFO** | 「いい感じ」（v0.37.1 で `pathTrace` 導入済み） | 変更なし | 引き続き、線と群れの軌跡の一致／指を速く動かしたときの遅れ |
| 🐸 **蛙竜** | 「**非常にいい感じ**」 | 変更なし | 跳躍と舌のリズム／射程66pxが短すぎないか |

**判定の出口**：
- **極端が効く** → Ⅲ群の残り（鬼火）→ Ⅳ・Ⅴ群へ。**さらに v0.35 の4種も振り幅を開き直す**。
- **極端すぎて破綻** → どのパラメータが破綻の主因かを1つ特定して、その手前で止める
  （爆竜なら `turnRate`、UFO なら等速 or `pathTrace` の隊列、蛙竜なら `hopPeriod`）。
- **極端でも変わらない** → 差別化の主軸を**攻撃側**に寄せる（species.md §8-5）。

※機械計測で確認済み（実機判断が要らない部分）：
- **爆竜の矢印操作**：45度になぞって離す→進路45.0度で確定、群れの実移動45.3度・10秒で1301px。
  タップは「重心→触れた点」と0.1度以内で一致。なぞっている最中は進路が変わらない（＝操縦されない）。
  左壁へ真正面から突っ込ませても最小速度76px/s（停止しない）、竜が世界の縁に触れたのは
  18,000サンプル中27フレームだけ。矢印は爆竜でのみ描画（他10種では出ない）。
- UFO は常時175px/sの等速。**リーダーは描いた線に完全に乗る（ズレ0）**、各竜のズレは平均38.5px
  （うち大半は隊列の幅±32px。火竜は159.9px・最大519px）。閉じた円は半径198〜200pxで周回。
- 爆竜は炸裂時に最寄りの竜が92px先（＝導火中に抜けきっている）。
- 蛙竜の舌は正面（0度）で命中・横（90度)/後方(180度)/射程外(100px)で不発を確認。
- 11種すべてで例外・NaN・震え・団子なし。`node tools/check.mjs` 通過。

### 2-1b. ステージ側の攻略要素（v0.41 で着手）

**ボス周辺の密度**（実機判定 2026-08-18「ボス周辺はもっと建物や敵を密集させたい。
建物密度は大きめの街と同等以上に」）に対応済み。次は §3-1 の攻略対象そのものの拡張へ。

| 変更 | 内容 |
|---|---|
| 城下の充填方式 | ランダム試行 → **同心リング上の候補列（六方最密相当）をシャッフルして詰める**。旧方式は密度を上げようとしても後半が全部弾かれ、棟数を増やしても密にならなかった |
| 充填範囲 | リング外側(188px)から → **ボス至近(68px)から城壁内側(400px)まで** |
| 間隔 | 32px → **22px**（街の small 同士は24px。首都は一段詰める） |
| 構成 | large リング＋small → **large リング＋medium(外周のみ)＋small**（街と同じサイズ構成にして「市街」に見せる） |
| 守備隊 | **新設**。`BOSS_GARRISON_BY_STAGE` で城壁の内側(200〜395px)だけを巡回する敵を配置。従来 `generateMovers` はボス圏内を明示的に避けており**ボス周辺に敵が1体も居なかった** |
| 民家の射撃 | **街と同じだけ撃つ**（`CAPITAL_SMALL_SHOOTER_RATE = 1.0`）。v0.41 では 0.06 まで絞ったが、**その結果ボス周辺が普通の街より弱くなった**（実機判定 2026-08-21「弱くする必要はない」）→ v0.43.1 で全戸射撃に戻した |

**踏んだ罠と、その後の揺り戻し（記録）**：密度を5倍にしたら**建物の射撃も5倍**になり、
群れ35体が2秒で全滅した（同一レイアウトA/Bで切り分け。原因は建物の射撃で、守備隊でも衝突でもない）。
そこで射手を6%まで絞ったが、**今度はボス周辺が普通の街より弱くなった**＝行き過ぎだった。
v0.43.1 で全戸射撃に戻し、射手の数はボス周辺147〜174／最も濃い街122〜180（比0.84〜1.22）＝**街と同等**。
そこへ要塞リングのカタパルト16基・城壁・守備隊・ボス本体が上乗せされるので**総合では街より明確に強い**。
教訓は「密度と弾量は連動する」であって「弾を減らせ」ではない。減らすのは
「城下は無防備な民家」という別の設計を選ぶときだけにする。

**v0.43.1 の実測（ボス突入シナリオ）**：
STAGE4・実プレイ相当（100体＋強化カード）＝**6.6秒でボス撃破**・329棟破壊・群れ90〜100体を維持。
STAGE8・同条件＝**44秒で撃破**・251棟破壊（12秒あたりで一度0体まで落ちてから90体へ復帰＝波状の攻城戦になる）。
STAGE4・強化なし60体＝29.4秒で撃破・146棟破壊（生存3〜33体で綱渡り）。

**実測（STAGE8・同一レイアウト・群れ60体固定）**：
現状は STAGE1/4 でボス撃破（群れ57/40体生存）。STAGE8 は強化なしの60体では落とせないが、
これは v0.40 相当（城下を撤去した状態）でも同じ＝**この変更が原因ではない**。
実プレイ相当（100体＋強化カード数枚）なら **40秒でボス撃破・291棟破壊・守備隊21体中19体撃破・群れ100体生存**。

### 2-1c. 攻略対象の拡張（v0.42 で着手）

実機判定（2026-08-20）で**採否がはっきり決まった**。以降この判断に従う。

| 案 | 判断 | 理由 |
|---|---|---|
| 収穫者（卵/XPを奪って逃げる） | **不採用** | 画面にXPがあまり残らない／**卵を取られるのは強い不快感**。「急ぐ理由」は別の手段で作る |
| 誘爆（火薬庫の連鎖爆発） | **敵側では不採用** | やるなら**竜側のスキル**として。敵の持ち物としては不要 |
| 鏡の盾兵 | **採用** | 実装済み |
| 分裂体 | **採用** | 実装済み |
| 潜行者 | **採用**（定番だが良い） | 実装済み |
| 持続する範囲攻撃 | **採用**（複数バリエーション。**建物側にも**） | 実装済み（敵2種＋建物1種） |

実装したもの：

| 要素 | 挙動 | 操作がどう変わるか |
|---|---|---|
| 🛡 **鏡の盾兵**（`shieldman`・4体1隊） | 進行方向±66度からの被弾を**完全に弾く**。追跡型なので盾は常に群れを向く | **回り込む**。横・背後からは通る（実測: 正面0／背後10／真横10ダメージ） |
| 🫧 **分裂体**（`splitter`） | 壊すと2体、その子もまた2体（1→2→4） | 早めに潰しきるか、無視して本命へ行くかの判断 |
| 🕳 **潜行者**（`lurker`） | 潜航中は**無敵・攻撃なし**（3.4秒）／浮上中だけ殴れる（2.6秒） | 浮上の瞬間に群れを乗せるタイミング合わせ。浮上直前は輪が強く脈打つ |
| 🔥 **火吹き車**（`flamewagon`） | **群れの足元**に燃える帯（半径110・3.6秒・5.5dps） | その場から退く。留まると効く |
| ☁ **瘴気塔**（`miasma`・静止） | 自分の周りに**広がる雲**（60→230・5秒・3.0dps） | 近寄れない領域ができる |
| 🗼 **焼夷塔**（建物・ステージごとに1〜8基） | large 建物の一部が周期的に燃える輪（半径145・4秒・4.0dps）を張る | 街の中に踏み込むと足元が燃える＝**建物側にも退かせる圧** |

**危険地帯（hazard）の共通仕様**：`spawnHazard(x,y,radius,life,dps,color,growFrom)` で誰でも置ける。
**必ず 0.7秒の予告**（輪が点滅・無害）が先に出る＝理不尽に見えない。ダメージは0.25秒刻み。
ステージ切替とクリア演出で消える。実測: 予告中0ダメージ→20体×6dps×1.5秒で**ちょうど180**。

**難度への影響（同一レイアウト・群れ60体固定のA/B）**：
STAGE1 は差なし（潜行者1体のみ）。STAGE4 は生存62→39だが**ボス撃破は維持**。
STAGE8 は生存28→0（ただし v0.42 以前でもボスHP85%残しで失敗しており、
強化なし60体では元から届かない）。**実プレイ相当（100体＋強化カード数枚）では
STAGE8 のボスを29.6秒で撃破・297棟破壊・群れは98体まで回復**。

**測定時の罠（記録）**：同一レイアウトA/Bで**前の試行がボスを倒すと `gameState` が
stage-clear に落ち、次の試行では竜の攻撃が全部止まる**（破壊数0の異常値になる）。
毎フレーム `gameState = "playing"` を強制してから測る。

### 2-1d. ダム／堰と雷撃塔車（v0.43）

next-steps §3-1 の残り推し順のうち上位2つを実装した。

| 要素 | 挙動 | 実測 |
|---|---|---|
| 🌊 **ダム／堰**（各ステージ1基・川の中流） | HPは large の6倍（約560）。**壊すと下流へ水の壁が走り、通った帯の建物が順に崩れる**（速度900px/s・帯の幅460px）。城壁とボスは流されない | 決壊から**2.3秒で419棟が水没**。「1つ壊すと広域が落ちる」＝俯瞰で最も映える枠 |
| ⚡ **雷撃塔車**（`teslawagon`） | 最寄りの竜に落ちてから**触れ合っている仲間へ幅優先で広がる**。1体ごとのダメージは**その竜の周りの密度に比例**（最大3.2倍） | 30px間隔で密集＝16体感電・1体あたり9.7／40px一列＝16体・5.6／95px散開＝**1体・3.4**。固めるほど痛い |

**雷撃塔車で作り直した点（記録）**：当初は「連鎖の長さ」で密集を罰する設計にしたが、
**Boids の群れは線に伸ばしても分離半径28前後で並ぶので、どう動かしても最大まで連鎖した**
（実測: 広がり300pxでも6連鎖）。連鎖の長さではなく**1体ごとの密度倍率**に変えて成立させた。
さらに2つ潰した：①落雷点が群れの端のはぐれ個体だとそこで途切れる（→隣を持つ竜を選ぶ）
②上限に達すると残りが無傷のまま終わる（→集める処理とダメージを配る処理を分ける）。

### 2-1e. 敵を確認するデバッグ機能（v0.44）

マップが 4800×3600 あるので、**特定の敵の挙動を見たいときに「探す」のが最大の手間**だった。
「呼ぶ・見分ける・落ち着いて眺める」の3つを用意した（デバッグパネル内。既定はすべてOFF）。

| 機能 | 内容 |
|---|---|
| 👾 敵を出す… | 図鑑（18種）から選ぶと**群れの300px隣に湧く**。隊で出る敵は隊ごと（盾兵×4・騎士団×8・マンモス×3）、巨獣はコア＋腕2本まとめて |
| 🏷 敵の名札 | 全ての敵の頭上に名前・HP・状態を出す。潜行者は「[潜航中/無敵]」、盾持ちは**弾く扇**を可視化 |
| 🛡 竜を無敵 | 群れが落ちないので敵の動きだけ見られる。ONの間はステータス行に【無敵ON】と出る（切り忘れ防止） |
| ☠ 敵を全部消す | 場を片付けて1体だけ観察する |
| 🌊 堰を決壊させる | 堰を探しに行かずに洪水を見る |
| 🎯 ボス手前へワープ | 群れ・操縦点・カメラごとボスの560px手前へ運ぶ（城下と守備隊の確認用） |

**設計上の注意**：パネルのボタンは `pointerdown` で即発火するので、**スクロールが要るとドラッグが
「湧かせる」に化ける**。図鑑は3列にして 375×812 で 356px（メインページは483px）＝どちらも
スクロール不要に収めてある。敵を増やすときはここが崩れないか確認すること。

### 2-1f. 見せ場になる敵の攻撃と、ステージごとの特徴（v0.48）

実機判定（2026-08-25）「もう少し印象的な敵の攻撃を増やしたい。予兆ありの極太レーザーを
撃つボスとか、弾速遅めのロケット弾をまとめて撃つ建物とか。その上でステージごとに
出る敵や建物の特徴を出したい」。

| 要素 | 挙動 | 実測 |
|---|---|---|
| ⚡ **極太レーザー**（ボス） | 予兆1.3秒（細い線が脈打つ・**無害**）→ 照射0.9秒（幅86・長さ1500・26dps）。予兆の時点で狙いが固定されるので横へ抜ければ避けられる | 予兆中のダメージ**0**／照射中561（群れ30体）／線から外れた群れは**0**。＝当たるかどうかは操作で決まる |
| 🚀 **ロケット砲台**（建物） | 5発を扇状（±0.42rad）に斉射。速度166px/s（矢は270）・AoE52・射程620 | 300px先まで**1.8秒**かかる＝飛んでくる間に群れを退かせる猶予がある |

**ステージごとの特徴**（`STAGE_FEATURES`）：敵の顔ぶれは以前からステージ別だったが、
**建物とボスの技が全ステージ同じ**で、どの時代でも同じ手触りだった。時代ごとに分けた。

| ステージ | 焼夷塔 | ロケット砲台 | ボスの極太レーザー |
|---|---|---|---|
| 1 石器 | 1 | — | — |
| 2 古代 | 3 | — | — |
| 3 中世 | 5 | — | — |
| 4 近世要塞 | 3 | **2（初出）** | — |
| 5 産業 | 5 | 5 | — |
| 6 電気 | 3 | 5 | **あり（初出）** |
| 7 大戦 | 6 | 8 | あり |
| 8 最終 | 8 | 10 | あり |

**踏んだ罠（記録）**：ロケットの発射処理を `targetIdx` の宣言より前に置いて
**const の TDZ で毎フレーム例外**になった。構文チェックは通るので、実際に撃たせるまで気づけない。
さらに **`read_console_messages` はリロードしても前の読み込みのエラーを出し続ける**ので、
「直したのにエラーが残っている」ように見えた。**確実に見るならページ内に error リスナーを
仕掛けて実ループを回す**（600フレーム・ロケット飛翔224フレームで例外0を確認した）。

### 2-1g. 文明ごとの兵器・看板ユニット・通常弾（v0.57）

実機判定（2026-09-02）「時代ごとにランが分かれることになったので、時代ごとに強い攻撃・
ユニットが欲しい。普通の建物が撃ってくる通常弾も時代ごとに適したものに変わってほしい」。

v0.56 で **1ラン＝1文明・時代はラン中ずっと固定**になった＝そのランで見る兵器はその文明の
ぶんが全部。だから時代の差＝ランの差になる。3層に分けて実装した。

#### (1) 通常弾（`SHOT_STYLES`・弾側が持つ）

ラン中ずっと画面を飛び続ける通常弾が、時代を語る一番大きな面積。形・速度倍率を文明ごとに変えた。
ダメージは `STAGE_CONFIGS` の `projectileDamage` のままで触っていない。

| 文明 | 弾 | 速度倍率 | 見た目 |
|---|---|---|---|
| 1 石器 | 投石 | 0.80 | 回転する不定形の石 |
| 2 古代 | 投槍 | 1.00 | 矢羽根なし・穂先が長い |
| 3 中世 | 弩の矢 | 1.05 | 従来の羽根つき矢 |
| 4 近世 | 鉛玉 | 1.50 | 小さい玉＋白煙のひと筋 |
| 5 産業 | 榴弾 | 1.15 | 黒い弾体＋煙 |
| 6 電気 | 放電弾 | 1.30 | 青白い芯＋ぎざぎざの尾 |
| 7 大戦 | 曳光弾 | 1.60 | 光の線そのもの |
| 8 最終 | プラズマ | 1.20 | コロナ＋残光 |

#### (2) 街の時代兵器（`EMPLACEMENT_DEFS` / `STAGE_FEATURES[].emplacements`）

large 建物のいくつかがこれになる。**竜に要求する操作**で選び分けている:
投石機/臼砲/対空砲＝落下点の円から退く、バリスタ/ロケット＝線から外れる、
焼夷塔/テスラ塔＝居座らない・固まらない、プリズム塔＝予兆の線を読む。

| 文明 | 兵器（数） | 実測（固定した35体・10秒・射程の9割の距離） |
|---|---|---|
| 1 石器 | 投石機4・焼夷塔1 | 投石機 340 |
| 2 古代 | 焼夷塔4・投石機2 | 焼夷塔 840（輪の中に居座った場合） |
| 3 中世 | バリスタ塔5・焼夷塔4 | バリスタ塔 38.7（線なので当たれば9発全部当たる） |
| 4 近世 | ロケット砲台3・バリスタ塔3・焼夷塔2 | ロケット砲台 66.2 |
| 5 産業 | 臼砲台5・ロケット砲台4・焼夷塔3 | 臼砲台 246.4 |
| 6 電気 | テスラ塔5・ロケット砲台4・焼夷塔2 | テスラ塔 277.2 |
| 7 大戦 | 対空砲6・臼砲台4・ロケット砲台4・テスラ塔2 | 対空砲 146.4 |
| 8 最終 | プリズム塔5・対空砲6・テスラ塔4・臼砲台4 | プリズム塔 201.6 |

#### (3) 首都の大技（`STAGE_FEATURES[].bossArt`）

どれも「予兆を見せてから当てる」。実測は**動かない35体**に当てた最大値で、
実際は群れを退かせれば避けられる（予兆中のダメージは全技で **0**）。

| 文明 | 大技 | 実測 |
|---|---|---|
| 1 石器 | 巨岩投擲（3発・AoE118・一番遅い） | 315 |
| 2 古代 | 神託の光柱（8スロット中7本・内外交互・1か所だけ空く） | 734 |
| 3 中世 | 矢の雨（34発・1.8秒かけて降る） | 75〜 |
| 4 近世 | 一斉射（13発×速度違い3波＝厚みのある弾の壁） | 60.2 |
| 5 産業 | 絨毯爆撃（首都から群れへ歩いてくる10発） | 200 |
| 6 電気 | 極太レーザー（v0.48から） | 819 |
| 7 大戦 | 対空弾幕（26発を2.2秒かけて撒く） | 165〜 |
| 8 最終 | 十字プリズム（4方向同時照射） | 324 |

#### (4) 文明ごとの看板ユニット

各文明に1体ずつ。攻撃の型は街の兵器と対にしてある。
**スプライトは既存の絵の流用**（サイズとオーラで差別化。v0.24 エリートと同じ方式）で、
専用の絵を起こすなら `ENEMY_IMAGE_SOURCES` / `ENEMY_SPRITE_META` に足して `sprite:` を外す。

| 文明 | ユニット | 攻撃 | 流用元 |
|---|---|---|---|
| 1 石器 | 長牙のマンモス | 踏鳴らし（半径175・押し70） | mammoth |
| 2 古代 | 黄金の巨像 | 眼光ビーム（9dps・射程320） | colossus |
| 3 中世 | 攻城塔 | 矢の面制圧（5本×2.2秒） | citadel |
| 4 近世 | 擲弾兵隊×5 | 爆弾（AoE46） | knights |
| 5 産業 | 装甲列車 | 舷側砲（4連×0.85秒） | ironclad |
| 6 電気 | 雷公 | 連鎖電撃（密度で最大3.4倍） | teslawagon |
| 7 大戦 | 爆撃飛行隊×4 | 爆弾（AoE50） | balloon |
| 8 最終 | 殲滅砲兵 | **着弾弾3発**（予告円つき・AoE70） | warlord |

#### (5) 新しい部品

- **着弾弾（`spawnShell` / projKind=4）**: 飛行中は当たり判定を持たず、**指定した地面で炸裂**する弾。
  落下点に予告円が出るので「線を外す」ではなく「**円から退く**」課題になる。
  投石・矢の雨・絨毯爆撃・対空弾幕・臼砲・殲滅砲兵が全部これ1つでできている。
- **斉射キュー（`queueShell` / `updateBarrageQueue`）**: 時間差で撒く。同時に落とすと避けようがない。
- **`emitChainLightning`**: 雷撃塔車の中にあった連鎖電撃を関数に切り出し、建物のテスラ塔と共有した。

#### 踏んだ罠（記録）

1. **`MAX_ATTACK_RADIUS = 380` が実効射程の上限だった**。建物の攻撃判定は
   「竜の位置から `_ATKCELL_RADIUS` セル内の建物を集める」逆引き方式なので、
   `attackRadius` をいくら広げても **400px より遠い群れは集合に入らず永久に撃たない**。
   プリズム塔（射程620）が496px の群れに10秒間0ダメージで発覚した。
   **v0.48 のロケット砲台（射程465と称していた）も実効400px だった**。
   → 走査半径を「置かれた建物の最大 attackRadius」から毎ステージ計算する
   （`recomputeAttackQueryRadius`、`cacheBossIndex` から呼ぶ）。射程は 520 までに揃えた
   （クエリ費用は半径の2乗で効くため）。
2. **首都の一斉射が届かなかった**。`spawnFanProjectileRaw` の catapult 弾は
   `PROJECTILE_MAX_RANGE=420` で消えるので、600px 先の群れに1発も届かない（実測0ダメージ）。
   → 個別の飛距離上限 `projRange` を追加した（0 なら kind ごとの既定値）。
3. **囲むだけの円は当たらない**。神託の光柱を半径168の円周に立てたら中心が安全地帯になり、
   居座った群れに**0ダメージ**だった。内側(62)と外側(140)を交互にして重心を覆う形に変えた。
4. **散らす技は測るたびに数字が暴れる**。矢の雨 27〜75、対空弾幕 18〜165。
   発数を増やして範囲を締めると安定する（34発/175px・26発/150px）。
5. **動員段階が上がると射程が消えた**。`reinforceSettlementForTier` が
   `attackRadius` を街の既定値に戻すため、段階1に上げた瞬間に投石機が 500→400 に縮んでいた
   （v0.48 の焼夷塔・ロケット砲台も同じ穴に落ちていた）。→ 補強のあとに兵器の射程を貼り直す。
6. 測定の作法: `applyDamageToBoid` を差し替えてダメージを積算し、**竜を毎フレーム同じ位置に置き直す**
   （`simStep` を呼ばない）。動かすと避けてしまい、技が機能しているのか当たっていないだけなのか分からない。

### 2-2. アセット（sprites.md §6 の作業順3以降）

- **隼竜・燕竜**（カテゴリA。既存の型どおり）
- **蜂竜**（カテゴリB＝小型高速。`DRAGON_FLAP_SPEED` の**種別化にコード変更が必要**）
- **ガーゴイル**（カテゴリC＝硬質。`flapFactor` を固定して翼の**回転角**を揺らす。コード変更が必要）
- **火竜アセットの角のアルファ掃除**（旧チョマキーのヘイズ 53/46 が残る）。
  掃除すると再クロップで寸法・ピボットが変わるので、**必ず再計測して `DRAGON_PART_SPRITES.fire` を更新**すること。

### 2-3. 竜種の追加（species.md §3 の残り4種）

Ⅲ群の残り（鬼火）→ Ⅳ群（雷雲竜・鶴竜・鮫竜）→ Ⅴ群（亀竜・鯨竜）。
**UFO は v0.37 で実装済み**（ただし動きだけ。見た目はベクター、攻撃は炎息の流用）。
UFO と鬼火の**アセット**はカテゴリD（無翼・`forward:"none"`・自転や明滅）で**コード変更が必要**。

### 2-4. 進化（攻撃の上位変化）

`ATTACK_DEFS` に A16〜A24 を足し、`SPECIES_DEFS.evolveTo` を実装する。
トリガーは既存の「素材カードを max まで取ると上位が出る」機構（next-steps 旧 §3-2 型D）に接続。
割り当て案は species.md §3 の「→進化」列（第1稿）。**竜種が3〜4種そろってから着手**する方針。

### 2-5. UI（竜種が増えると必須）

- 選択メニューは v0.37 で**2列グリッド**にした。**13種＋もどるで縦619px（375×812の画面に対し余白16px）＝
  次の1種でもう入らない**。次に竜種を足すときは先に3列化かアンロック制の実装が要る。
  **アンロック済みのみ表示**は未着手。
  解放は `CHALLENGE_DEFS`／`evaluateChallenges` に接続する。
  ※スクロールにしなかったのは、カードが `pointerdown` で即選択される作りなので
  スクロールのドラッグと衝突するため。
- **専用カード**：`CARD_DEFS` の `weapon:` フィルタを `species:` に拡張（未着手）。

---

## 3. 未着手のアイディア（着手順は未定）

### 3-1. 攻略対象の拡張（本書 §5 に詳細を保管）

**v0.42 で盾兵・分裂体・潜行者・持続範囲攻撃を実装済み（§2-1c）。収穫者と誘爆は不採用が確定。**

**v0.43 でダム／堰と雷撃塔車も実装済み（§2-1d）。**

残りの推し順：**囚われの竜**（檻を壊すと特別個体が1ランだけ仲間に）→ **竜檻／卵倉庫**
→ **神殿の結界柱（3本）**（3本すべて壊すまで本殿が無敵＝群れを分ける段取り）
→ **修復工兵**（壊した建物を建て直す＝破壊順の設計）。
※「急ぐ理由」は収穫者が不採用になったので、**避難民の列**や**交易キャラバン**
（逃げ切られると取り逃す）で作る方向に切り替える。

### 3-2. シナジーの土台

現在のカードは全部 `eff*` を通る独立した掛け算なので、構造的にシナジーが生まれない。
候補は **①サブ武器スロット** と **②状態異常（燃焼・凍結）＋起爆**。
型は4つ（状態異常＋起爆／閾値条件／変換／進化合成）。

### 3-3. 選択そのものを面白くする（Balatro的な引き）

リロール（ラン中N回／竜晶消費）・バナー（二度と出さない）＝ビルド純度・
呪い付き強カード（威力+80%／群れ上限-20 等）・star枠の当たり感の演出強化。

### 3-4. メタ側の残タスク

- **チャレンジ報酬に特別解放ノード**（`unlockBy: challengeId`／竜晶では買えない）＝
  「竜晶＝量の育成／チャレンジ＝腕前で解く特別ノード」の**二層経済を閉じる**。
  `treeNodeState` に "challenge-locked" 追加、`purchaseTreeNode` でブロック、`evaluateChallenges` で解放。
- **各ラインの名物ノード**（母竜=虹色特大ブレス／大群=一定数超えで群れが黄金化しXP+／
  統率=軌跡が光る彗星の尾／恵み=クリア時に竜晶が花火のように舞う）。
- **母竜ブレスの進化（枝分かれ）**：直線貫通／分裂／範囲凍結／なぎ払い。
- 数値バランスの実機チューニング。

### 3-5. グラフィカルな見せ場（俯瞰で映える演出）

**推し3つ**：**星座の網**（近い竜同士が光の線で結ばれ、群れが増えるほど網が濃く美しい＝
3本柱に最も忠実・実装軽い）／**龍脈**（枝分かれする溶岩の亀裂が残り冷えて黒く沈む）／
**天焦がし**（警告円→白熱の光柱＝溜めと解放）。
他：焔の軌跡・溶岩湖・氷華・極光の帯・鯨波・蝕・群れの陣・渦潮・星降り・プリズム・黒星・雷雲。

### 3-6. 操作の残課題（controls.md §7）

- **7-2 リーダー引力に距離減衰がない**（`wLeader` は方向のみ）。到達減速は入れたが、
  「群れが伸びきる」感覚が残るかは実機判断。
- **7-4 細かい操作が捨てられる**：`PATH_MIN_SEGMENT = 30`（ワールドpx）。
  小さく速い曲線がパスにならない。グレイズや細い路地の走破をやるなら要調整。
- **7-5 カード選択中、画面下部中央が押せない**（カード3枚183px）。
- **7-6 マルチタッチで操縦点が飛ぶ**（常に `e.touches[0]` を見ている）。
- **7-7 死にコード**：`inputMode = "UI"` は `bottomUIHeight === 0` で到達不能。
  カメラ追従 `* 0.08` に `dt` が掛かっていない（低fps端末で追従が遅れる）。

---

## 4. 恒久ルール・制約（必ず守る）

- **トップビュー厳守**：ドーム/立面など横視点の形は禁止（干し草をドームにして差し戻した実績あり）。
- **敗北要素なし**：竜はダウンしても必ず復帰。ノーダウン系のチャレンジもコンセプト違反として不採用。
- **モーダルでプレイを止めない**：カード選択はスローモーで非モーダル。
- **音は当面後回し**。
- 「被り」（スタジオ内・過去の種・市場の先行作）は設計・判定の制約にしない。
- 変更は毎回 `/deploy-dragon-tide` で検証→master 反映。URLは装飾なしのプレーンテキストで単独行に。
- 全ユーザー向け文字列は `STRINGS` 経由、数値は定数に集約（マジックナンバー禁止）。
- **移動パラメータを名前付きプリセットに畳まない**（`move:"standard"` のような enum を作らない）。
  分離した目的がバリエーションなので、竜種ごとに全項目を自由に組み合わせられる形を保つ。
- **ピボット値の正典はコード内の定数**（`DRAGON_PART_SPRITES`）。`data/` へ外出ししない
  （v0.35.1 で JSON を削除。読まれないデータは腐る）。

### 検証の既知の罠（時間を溶かした実績あり）

- **プレビューが `data:` スナップショット化して相対パスの画像を読めない**ことがある。
  この状態では全竜種がベクター描画に落ちて見える＝**コードのバグと誤認しやすい**。
  `location.href` が `data:` で始まっていないか必ず確認する。
- `preview_screenshot` はタイムアウトする。見た目はユーザーの実機に委ねる。
- プレビューでは canvas サイズ0（`viewH=0`）。入力テストは `viewW/viewH` を390/844にしてから、
  または `isDrawing`／`touchRing`／`pathPush` を直接立てて `simStep(dt)` を回す。
- **`beginRun(weapon)` は `gameState !== "title"` で即 return する**。竜種を切り替えて比較するときは
  `flockSpecies` を直接代入して `resetBoidsForStageStart(n)`。
- **`mv()` は `moveProfileOverride|flockSpecies` をキーにキャッシュする**。同じキーのまま
  プリセットを書き換えてパラメータを振ると全条件が1本目の値で走る（スイープを丸ごと誤測定した実績あり）。
- **旋回性能は「◯度旋回にかかる時間」で測らない**。リーダーの方位が群れの移動につれて変わるため。
  1フレームごとの実回転量を積んで角速度を出す。180度ちょうどは左右どちらに回るか決まらない特異点。
- **竜種を順に走らせて掃討速度を比べると、先の種が町を壊して後の種が不利になる**。
  建物の hp/fallen を毎回復元してから測る。
- 構文チェックは `<script>` 内JSを**BOMなしUTF-8**で抽出して `node --check`。
  ただし **`node --check` / `tools/check.mjs` は構文しか見ない**。`const` の TDZ
  （宣言前の定数を参照）はロード時に初めて落ちるので、**編集後は必ずページを開いて
  コンソールを確認する**（v0.41 で `BOSS_RADIUS` を宣言前に参照して真っ白になった）。
- `generateStageLayout(idx)` は**返り値**で建物配列を返す（グローバル `settlements` を更新しない）。
- **震えの計測は個体0だけ見ない**。群れ全員の1フレームあたり回転量を平均する
  （個体0がたまたま輪の内側にいると正常でも異常でも数字が振れる。実際に一度誤判定した）。
- **回り込み・周回の「向き」を個体ごとに決めると半数が逆回りして正面衝突する**（v0.37）。
  群れの角運動量で全員揃える。詳細は controls.md §12-2d。
- **即時方向転換の竜は、分離を"向き"に混ぜると必ず震える**。分離は外力チャネル（kick）へ回す。
- プレビュー（このリポジトリの `preview_start`）では **`computer` のスクリーンショットも
  タイムアウトする**（"Browser pane is not displayed"）。見た目はユーザーの実機に委ねる。
  ただし **canvas の `getImageData` によるピクセル計測は動く**（跳躍の描画倍率もこれで確認した）。

---

## 5. 攻略対象（敵・建物・地形）の拡張案

> **視点**：攻撃の図形が「こちらの手」なら、攻略対象は「相手の課題」。
> 現状の敵は挙動が「撃つ／照射／衝撃波／回復／召喚」に収束し、**どれも群れをまっすぐ
> 突っ込ませれば解ける**＝操作の課題を変えていない。ここが最大の空白。
> **敗北はないので、脅威は「負ける」ではなく「成長が遅れる／取り逃す／手間が増える」として設計する。**

### 5-1. 既出だが未実装（GDD§13 から拾い直し）

| 案 | 内容 |
|---|---|
| **閾値ゲート** | 建物にサイズ関門。群れが一定数を超えると**大建築が一気に溶ける**。「すごく気持ちいい」の本体とされながら未着手 |
| **穀倉** | 壊すと竜が **+10体即孵化**。破壊が直接成長になる |
| **火薬庫** | 誘爆で周囲一掃。花火要塞との差別化は**連鎖が隣の建物へ伝播**する点 |
| **時代のワンダー** | ステージの目玉となる巨大建築（ピラミッド／大聖堂／大時計塔） |
| 属性相性を要求する建物 | 属性を作る前に**刺さる相手が先**という設計順序がGDDに明記されている |
| ランクアップ演出 | 規模が段階を超えると称号＋フラッシュ＋カメラパンチ |

### 5-2. 移動敵（★＝推し）

| 案 | 挙動 | 操作がどう変わるか |
|---|---|---|
| **★収穫者** | 落ちた卵/XPジェムを**回収して逃げる** | 資源を直接奪うので放置できない。**初めて「急ぐ理由」ができる** |
| **★鏡の盾兵** | 正面反射、**背後からのみ通る** | **回り込む操作**。線を大きく迂回させる |
| **★雷撃塔車** | 最寄りの竜から**群れ全体へ連鎖**する電撃 | **群れを固めると損**。Boidsでしか成立しない課題 |
| 修復工兵 | 壊した建物を**建て直す** | 進行が巻き戻る。破壊順の設計 |
| 分裂体 | 壊すと2体に分かれる | 早期に潰すか無視するかの判断 |
| 猟犬隊 | 執拗に追尾 | **振り切る操作** |
| 投網兵 | 竜を数体その場に**縛る** | 群れが引き裂かれる＝**置き去りを取りに戻る**往復 |
| 潜行者 | 浮上した時だけ攻撃が通る | タイミング合わせ。滞在型と噛む |
| 装甲列車 | 線路上を高速往復 | **併走して舐める**操作 |
| 旗持ち | 周囲の敵を強化 | 優先順位の操作（移動する healer） |
| 人類の英雄 | 高速で突っ込み竜を1体ずつ落とす | 少数精鋭系のアンチ。倒すと大報酬 |
| 囮の卵運搬車 | 追うと伏兵が湧く | **読み**。世界が仕掛けてくる |
| 攻城塔 | 脚を折ると倒れて自壊 | 部位破壊の段取り |

### 5-3. 建物・構造物

| 案 | 内容 |
|---|---|
| **★ダム／堰** | 壊すと**下流の町が水没して一斉崩壊**。一撃で広域が落ちる最大級の快感。俯瞰で最も映える |
| **★竜檻／卵倉庫** | 奪われた卵が大量に見える建物。壊すと卵が一気に解放。世界観と直結 |
| **★神殿の結界柱（3本）** | 3本すべて壊すまで本殿が無敵。**群れを分ける／順に回る**段取り |
| 城門・跳ね橋 | 壁のある町の"鍵"。壊すと壁が崩れる |
| 水道橋／導水路 | 長い**線状**の建造物。端から端まで舐めると連鎖崩壊 |
| 鐘楼 | 壊すまで周囲の敵の攻撃が速い |
| 対空櫓 | 長射程・単発高ダメージの固定砲。「先に潰す」動機 |
| 灯台／監視塔 | 壊すまで群れの位置が敵に共有される |
| 鉱山／採掘場 | 壊すと**竜晶**が大量に出る |
| 避難民の列 | 移動する"建物群"。逃げ切られると取り逃す |

### 5-4. 地形・環境

現状 forest/field は**純粋な装飾**。ここを起こすと「世界を書き換えている」感が上がる。

| 案 | 内容 |
|---|---|
| **★延焼する森・油田** | 火竜のブレスで着火し**風下へ燃え広がる**。届かない町へ火が回る |
| 氷結湖 | 氷竜で凍らせると渡れる／割ると上の敵が落ちる |
| 溶岩の亀裂 | 開くと敵の進路を塞ぐ |
| 竜脈 | 地面の下を走る線。舐めると竜晶。**俯瞰で見える隠し資源** |
| 橋の破壊 | 壊すと敵の進路が変わる |
| 雷雲 | 通り抜けると帯電バフ |

### 5-5. 報酬・イベント枠

| 案 | 内容 |
|---|---|
| **★囚われの竜** | 檻を壊すと**特別個体が1ランだけ仲間に** |
| **★竜の墓標** | 触れるとラン中カードが1枚もらえる |
| 卵運搬船団 | 大量の卵を運ぶ編隊 |
| 祭りの日 | 祝祭状態＝花火要塞が多いが警備も厚い |
| 交易キャラバン | 地図を横断。逃す前に捕まえる時間制限 |

### 5-6. ボス／ワンダー級

空中要塞（先に地上の対空櫓を潰す必要）／大聖堂（結界柱をボス規模で）／
機械仕掛けの巨神（behemoth の上位・パーツ4本）／人類連合軍（全敵種が一斉＝`updateEnemyRush` の拡張）。

### 5-7. 実装の差し込み口

- 移動敵：`MOVER_DEFS` に定義追加 → `MOVER_SPAWNS_BY_STAGE` に配置。攻撃は `updateMovers` の
  `attack.type` 分岐（beam/shockwave/fan/broadside/bomb/heal/summon）に新タイプを足す。
  多パーツは `updateBehemothArm`、逃走は `fleer`、飛行は `flying`、報酬は `treasureReward`。
  ※**`drawMovers` は `def.attack.type` を無防備に参照する**ので、攻撃を持たない mover を足すときは要注意。
- 建物：`generateStageLayout` の返り値に混ぜる。破壊フックは `settlementFall`、
  特殊建物の雛形は `isFirework` / `detonateFirework` / `isEggBearer`。
- 地形：`generateForests` / TERRAIN 系。当たり判定を持たせるなら `buildingGridQueryRadius` に載せる。

---

## 6. 実装の差し込み口（竜種・操作・メタ）

| 目的 | 差し込み口 |
|---|---|
| 竜種の追加 | `SPECIES_DEFS`（攻撃×動き×見た目）／切替は `setFlockSpecies(id)`／選択肢は `SPECIES_CHOICES` |
| 攻撃の追加 | `ATTACK_DEFS`（kind・recoilDist・fireStun）＋ `updateWeaponAttacks` の kind 分岐。<br>範囲攻撃は `damageInRadius`、投射は `spawnDragonProjectile`、斬撃FXは `pushSlashFx`、リングFXは `shockwaveFxList` |
| 動きの調整 | `MOVE_PROFILE_DEFAULT` ＋ 各 `SPECIES_DEFS[].move`。取得は `mv()`。<br>共通定数 `TURN_GAIN` / `TURN_DEADZONE` / `BOID_ARRIVE_DIST` / `SEP_MIN_SPEED_FRAC` |
| 回り込み（v0.37） | `orbitRadius`（竜種）＋ `_orbitSide`・`ORBIT_SIDE_HYSTERESIS`・`ORBIT_SEP_KICK_ACCEL`・`ORBIT_RING_ROWS`。パス末端側は `pathEndMode:"orbit"` と `PATH_END_ORBIT_RADIUS` |
| 跳躍（v0.37） | `hopPeriod` / `hopFlightFrac` / `hopGroundFrac` / `hopPathSkip`（竜種）＋ 共通時計 `hopClock`・位相ずらし `HOP_PHASE_STAGGER`。描画は `boidHopAir[i]` と `HOP_DRAW_SCALE` |
| 弧長トレース（v0.37.1） | `pathTrace`（竜種）＋ `pathBuildCum` / `pathPointAtArc` / `leaderArc` / `traceIdle`。隊列は `PATH_TRACE_*`（列数は生存数から算出） |
| 操作方式の切替（v0.39〜） | `controlScheme`（竜種）。"vector" は `updateLeaderVector`（進路の決定）＋ `deflectVectorBearingAtWall`（縁の処理）＋ `drawVectorAim`（矢印）。状態は `vectorBearing` / `vectorAiming` / `vectorCommitFx` / `vectorShowTimer`、定数は `VECTOR_*`。<br>**同じ動き・同じ攻撃で操作方式だけ違うペア**＝爆竜（vector）と惑竜（path） |
| 爆撃・舌撃（v0.37.1） | `updateBombDrops` / `updateBombFuses` / `drawBombs`（`bombList`・`BOMB_*`）／`updateTongueAttacks` / `drawTongueFx`（`tongueFxList`・`TONGUE_*`）。どちらも `updateWeaponAttacks` の kind 分岐に接続 |
| 反動・ノックバック | `applyKick` / `applyKickDist`（減衰する外力）。`KICK_DECAY_PER_SEC` |
| スプライト | `DRAGON_PART_SPRITES`（パーツ座標）／`getSpeciesSprites(id)`（種IDキーLRU）／`DRAGON_ELEMENT_COLORS`（パレット） |
| 操作 | `inputStart`/`inputMove`/`inputEnd`／`updateLeader`（3モード）／`pathPush`／`isPathClosed` |
| ラン中強化 | `CARD_DEFS`／文言は `STRINGS.cards`／抽選は `buildLevelUpCards`／取得は `chooseCard`＋`applyCardImmediate` |
| 効果の適用ハブ | `eff*` 関数群。恒常バフは `permaBuff` が合流 |
| 恒常ツリー | `TREE_DEFS`／`recomputePermaBuff`／`purchaseTreeNode`／UIは `renderTree` |
| チャレンジ | `CHALLENGE_DEFS`／`evaluateChallenges`／UIは `renderChallenges` |
| 敵 | `MOVER_DEFS`／`MOVER_SPAWNS_BY_STAGE`／`updateMovers`／`damageMover`／ラッシュは `updateEnemyRush` |
| ステージ生成 | `generateStageLayout`／`placeTownCenters`／`generateTownWalls`／`START_TOWN_CLEAR` |
| デバッグ | 左下バージョン3タップ。「🛸 動き切替」（hover/ground）「💥 反動切替」（0/40/100/240）<br>**v0.44 の敵確認セット**: 「👾 敵を出す…」（図鑑から選んで群れの隣に湧かせる。隊は隊ごと・巨獣は腕つき）／「🏷 敵の名札」（名前・HP・潜航中かを頭上に表示、盾持ちは弾く扇も出る）／「🛡 竜を無敵」（落ちずに敵の挙動だけ観察）／「☠ 敵を全部消す」（1体だけ残して見る）／「🌊 堰を決壊させる」／「🎯 ボス手前へワープ」 |
